[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncua>=1.1.8",
    "bcrypt>=5.0.0",
    "fastapi[standard]>=0.135.1",
    "sqlmodel>=0.0.37",
    "vllm>=0.16.0",
]
